#ifndef FUNCTION_H
#define FUNCTION_H

// sometimes we want to keep parameters to a function for later use
// this is to avoid the 'unused parameter' compiler warning
#define UNUSED(x) (void)(x)

#endif
